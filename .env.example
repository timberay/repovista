# RepoVista Configuration Example
# Copy this file to .env and update with your values

# ============================================================================
# Docker Registry Configuration
# Point this to your EXISTING Docker Registry
# RepoVista does NOT manage or run the Registry itself - it's a read-only UI client
# ============================================================================

# The URL of your Docker Registry (v2 API)
# Examples:
#   - Private Registry: https://registry.your-company.com
#   - Harbor: https://harbor.your-company.com
#   - AWS ECR: https://123456789.dkr.ecr.region.amazonaws.com
#   - Local Registry: http://localhost:5000 (for development only)
REGISTRY_URL=https://registry.example.com

# Registry Authentication (optional)
# Leave empty for public registries or registries without authentication
REGISTRY_USERNAME=
REGISTRY_PASSWORD=

# CORS Configuration
# Comma-separated list of allowed origins
# Add your production domain here
CORS_ORIGINS=http://localhost,http://localhost:8082

# Service Ports
# ============================================================================
# Port Configuration (using non-standard ports to avoid conflicts)
# - Backend: Host port 3032 → Container port 8000
# - Frontend: Host port 8082 → Container port 80
# 
# For local development without Docker, use the same host ports:
# - Backend: uvicorn backend.main:app --port 3032
# - Frontend: python -m http.server 8082
# ============================================================================

# Backend API port (internal container port, DO NOT CHANGE)
API_PORT=8000

# Frontend web server port (internal container port, DO NOT CHANGE)  
FRONTEND_PORT=80

# Note: To change the exposed host ports, modify docker-compose.yml or docker-compose.override.yml
# Default host port mapping: 3032:8000 (backend), 8082:80 (frontend)

# Optional: Logging Configuration
# LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Optional: Cache Configuration
# CACHE_TTL_REPOSITORIES=300  # seconds
# CACHE_TTL_TAGS=300  # seconds
# CACHE_CLEANUP_INTERVAL=60  # seconds