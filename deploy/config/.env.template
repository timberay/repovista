# RepoVista Configuration
# Please update these values according to your environment

# Docker Registry Configuration
# The URL of your Docker Registry (e.g., http://registry.company.local:5000)
REGISTRY_URL=http://localhost:5000

# Registry Authentication (leave empty if not required)
REGISTRY_USERNAME=
REGISTRY_PASSWORD=

# Service Ports
# Frontend web interface port
FRONTEND_PORT=80

# Backend API port
API_PORT=8000

# CORS Settings (automatically configured by install script)
# Add your server IPs or domains separated by comma
CORS_ORIGINS=http://localhost

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Mock Data Mode
# Set to true to use mock data when registry is unavailable (for testing)
USE_MOCK_DATA=false